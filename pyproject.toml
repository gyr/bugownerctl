[project]
name = "bugownership-utils"
version = "0.1.0"
description = "Utility scripts for validating bug ownership and package maintainership."
dependencies = [
    "lxml",
    "PyYAML",
    "requests",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
